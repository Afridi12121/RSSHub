{
  "version": 2,
  "build": {
    "env": {},
    "installCommand": "npm install --no-optional",
    "buildCommand":   "npm run build"
  },
  "routes": [
    { "src": "/(.*)", "dest": "dist/index.js" }
  ]
}
